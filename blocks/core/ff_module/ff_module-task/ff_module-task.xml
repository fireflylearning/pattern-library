<notice modifier="{{ modifier }}" id="{{ id }}" sent_to="{{ sent_to }}" no_excused="{{ no_excused }}" handedin="{{ completed_by }}" marked="{{ marked }}" from="{{ from }}" link_href="{{ link_href }}" to="{{ to }}" duedate="{{ duedate }}">
    <message>{{ message }}</message>
    <htmlMessage>{{ message }}</htmlMessage>
    <description/>
    <attachments/>
</notice>