<planner title="{{planner.title}}" dateUrlPrefix="{{planner.dateUrlPrefix}}" nowUrl="{{planner.nowUrl}}" previousUrl="{{planner.previousUrl}}" nextUrl="{{planner.nextUrl}}" dayUrl="{{planner.dayUrl}}" weekUrl="{{planner.weekUrl}}" isNow="{{planner.isNow}}"/>
