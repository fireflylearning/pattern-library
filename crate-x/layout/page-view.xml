<?xml version="1.0" encoding="UTF-8"?>
    <?xml-stylesheet type="text/xsl" href="{{name}}.xsl"?>

        <page xmlns:ff_module="http://www.fireflylearning/module">
            <title>{{title}}: {{name}}</title>
            <content>{{contents|safe}}</content>

            <blocks>
                {% for block in blocks %}
                {% for name, contexts in block %}
                {% for context in contexts %}
                {% set p = '../../blocks/core/ff_module/' + name +'/' + name + '.html' %}
                <block link="{{name}}" id="{{name}}">
                    {% include p with context only %}
                </block>
                {% endfor %}
                {% endfor %}
            </blocks>
        </page>
