<label modifier="{{modifier}}" for="{{forId}}" required="{{required | default('true')}}">
    <text>{{text | default('Label text')}}</text>
    {% for d in data %}
    <data attr="{{d.attr}}">{{d.value}}</data>
    {% endfor %}
</label>
