<tabs>
    {% for tab in tabs %}
        <tab state="{{tab.state | default('')}}" id="{{tab.id}}">
            <label>{{tab.label | default('Tab Content')}}</label>
        </tab>
    {% endfor %}
</tabs>
