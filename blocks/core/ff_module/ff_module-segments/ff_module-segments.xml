<items modifiers="{{ modifiers }}">
    {% for item in items %}
    <item url="{{item.url | default('#')}}" active="{{item.active | default('false')}}">{{item.name | default('Item One')}}</item>
    {% endfor %}
</items>
