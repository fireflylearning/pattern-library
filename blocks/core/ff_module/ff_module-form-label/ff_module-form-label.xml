<label modifier="{{modifier}}" for="{{id}}" required="{{required | default('true')}}" classes="{{classes}}">
    <text>{{text | default('Label text')}}</text>
    {% for d in data %}
    <data attr="{{d.attr}}">{{d.value}}</data>
    {% endfor %}
</label>
